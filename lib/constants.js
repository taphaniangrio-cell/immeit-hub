const CONSTANTS = {
  // Timeouts (ms)
  FETCH_TIMEOUT: 4000,
  RSS_GLOBAL_TIMEOUT: 15000,
  AI_REQUEST_TIMEOUT: 90000,
  IMAGE_FETCH_TIMEOUT: 5000,
  PEXELS_API_TIMEOUT: 8000,
  SHAREPOINT_REQUEST_TIMEOUT: 30000,
  SHAREPOINT_HTTPS_TIMEOUT: 30000,
  AUTO_SYNC_TIMEOUT: 90000,
  SERVER_REQUEST_TIMEOUT: 120000,
  FRONTEND_FETCH_TIMEOUT: 120000,

  // Caches TTL (ms)
  RSS_CACHE_TTL: 30 * 60 * 1000,
  IMAGE_CACHE_TTL: 120 * 60 * 1000,
  SHAREPOINT_TOKEN_CACHE_TTL: 5 * 60 * 1000,
  AUTO_SYNC_REFRESH_INTERVAL: 60 * 1000,
  SESSION_TTL: 7 * 24 * 60 * 60 * 1000,
  RATE_LIMIT_CLEANUP_INTERVAL: 300_000,

  // Rate limits (requests per window)
  RATE_LIMIT_AUTH: { max: 10, windowMs: 60_000 },
  RATE_LIMIT_ARTICLES: { max: 30, windowMs: 60_000 },
  RATE_LIMIT_GENERATE: { max: 5, windowMs: 60_000 },
  RATE_LIMIT_NEWS: { max: 20, windowMs: 60_000 },
  RATE_LIMIT_IMAGES: { max: 30, windowMs: 60_000 },

  // Payload limits
  MAX_PAYLOAD_SIZE: 1_000_000,

  // DB
  DB_CONNECTION_TIMEOUT: 5000,
  DB_POOL_MAX: 3,
  DB_QUERY_RETRIES: 2,
  DB_RETRY_BASE_DELAY: 100,

  // Pagination
  ARTICLES_PAGE_SIZE: 50,
  DASHBOARD_TABLE_ROWS: 100,

  // Frontend
  LINKEDIN_TARGET_WORDS: 1500,
  ARTICLE_LIST_PAGE_SIZE: 10,

  // Security
  BCRYPT_SALT_ROUNDS: 12,
};

module.exports = { CONSTANTS };
